@model KKBusWebApp.Models.PRACOWNICY

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>PRACOWNICY</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.OSO_ID, "OSO_ID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("OSO_ID", String.Empty)
                @Html.ValidationMessageFor(model => model.OSO_ID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.PRA_UPRAWNIENIA, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.PRA_UPRAWNIENIA)
                @Html.ValidationMessageFor(model => model.PRA_UPRAWNIENIA)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
