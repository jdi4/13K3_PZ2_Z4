@model IEnumerable<KKBusWebApp.Models.PRACOWNICY>

@{
    ViewBag.Title = "Index";
}

<h2>@ViewBag.StatusMessage</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PRA_UPRAWNIENIA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.OSOBY.OSO_IMIE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PRA_UPRAWNIENIA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.OSOBY.OSO_IMIE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PRA_ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PRA_ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PRA_ID })
        </td>
    </tr>
}

</table>
